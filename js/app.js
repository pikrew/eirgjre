const burger = () => {
    const btn = document.querySelector(',burger svg:last-child')
    const off = document.querySelector(',offset')
    const close = document.querySelector('.offs')
    btn.addEventListener('click', () => {
        
    })
}